<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.websarva.wings.android.gourmet_search_app">
    <!-- 位置情報の許可 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- ネットワーク通信の許可 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gourmet_Search_App"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".backEnd.TutorialActivity"
            android:theme="@style/WelcomeScreenTheme"  />
        <activity android:name=".flontEnd.StoreDetailActivity" />
        <activity android:name=".flontEnd.Search_ResultActivity" />
        <activity android:name=".flontEnd.GPSActivity" />
        <activity android:name=".flontEnd.Range_SelectionActivity" />
        <activity android:name=".flontEnd.Input_APIkeyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>